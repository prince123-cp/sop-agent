# TODO: SOP Components Implementation

## Tasks
- [x] Update SopUpload.jsx: Fix FormData keys to match backend ('name' instead of 'sopName', 'pdf' instead of 'file')
- [x] Update SopList.jsx: Add "Uploaded Date" column to table, format date, and implement empty state "No SOPs uploaded yet"
